class Interactions {
    constructor(editor) {
        this.editor = editor;
        this.isShowing = true;
        this.buildInteractions;
    }

    buildInteractions() {
        return "Interactions Manager";
    }
}

export default Interactions;
class Logic {
    constructor(editor) {
        this.editor = editor;
        this.isShowing = true;
        this.buildLogic();
    }

    buildLogic() {
        return "Logic manager";
    }
}

export default Logic